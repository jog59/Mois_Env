// animation.js — vidé : la logique d'animation des icônes est maintenant
// gérée directement dans detection.js via createHotspot({ icon: { ... } })
console.log("animation.js chargé (logique déplacée dans detection.js)");
